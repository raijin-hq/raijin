[project]
name = "raijin-eval"
version = "0.1.0"
description = "Harbor agent wrapper for Raijin's eval-cli"
requires-python = ">=3.12"
dependencies = ["harbor"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
